# Development and release-validation dependencies.
# Runtime packages remain centralized in requirements.txt.
-r requirements.txt

# Python browser automation used by tests/browser and
# tests/run_final_integration_checks.py --require-runtime.
playwright>=1.50.0,<2.0.0

# Test runner used by the GitHub Actions runtime gate.
pytest>=8.0,<9.0
