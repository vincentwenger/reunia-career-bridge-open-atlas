Réunia Career Bridge — Lightsail deployment validation script
Date: 2026-07-30

Implemented
-----------
- scripts/deployment/validate_lightsail_deployment.py and its Windows wrapper are present.
- Requires /health to report persistent DynamoDB/DynamoDB/S3 storage by default.
- Accepts demo storage only with the explicit validator override.
- Enforces Lightsail scale 1 and one Gunicorn worker only for demo storage.
- Accepts positive multi-node and multi-worker settings for persistent storage.
- Rejects any live Lightsail container command override in every mode.
- Validates that the image command starts Gunicorn with positive worker/thread counts.
- Signs in with a dedicated validation account, creates and retrieves a uniquely
  named Job Application, and removes the smoke-test record afterward.
- Verifies the deployment documentation contains the prominent conditional
  redeployment data-loss warning.

Validation performed
--------------------
Command: python -m unittest discover -s tests -v
Result: 142 tests completed; 126 passed; 16 skipped; 0 failed.

The skipped tests require optional Flask, Redis, OpenAI, and/or XLRD runtime
dependencies that were unavailable in the validation environment.

Live AWS status
---------------
The validator was not run against the user's live AWS account or public URL in
this offline environment. Run scripts/deployment/validate_lightsail_deployment.bat
after deployment with CAREER_BRIDGE_BASE_URL, DEPLOYMENT_VALIDATION_EMAIL, and
DEPLOYMENT_VALIDATION_PASSWORD configured.
