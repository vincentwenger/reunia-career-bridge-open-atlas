# Generated validation reports

Files in `reports/validation/` are generated by test and validation commands. They may describe the package state at the time they were produced and can become historical after later changes.

Use the current source code, `docs/`, and a fresh run of `tests/run_final_integration_checks.py` as the source of truth. Do not treat a bundled feature-specific text report as a current product specification.
